🛑 MARKDOWN CONTENT IS HANDLED SEPARATELY — DO NOT GENERATE IT HERE:
- A separate chunk-by-chunk conversion pass has already produced this document's full Markdown representation (tables, headings, everything). Do NOT include a "markdown_content" field in your JSON response, and do NOT spend effort reconstructing tables or full document formatting — it is discarded either way.
- Your ONLY job in this step is: Category/Subcategory classification, the Executive Summary, and the structured metadata fields (titre, registre, date, tags, total_amount, vat_amount, siren, iban, expiry_date, contact_*).
- FUSED OCR TEXT DE-CONCATENATION (for the fields above only): If the raw document text contains fused/concatenated words, jammed numbers, or run-together labels (e.g. "Invoice#INV2405030068", "Totalpayable€12.98"), mentally split them into clean words before extracting titre/summary/amounts/contacts — do not let jammed OCR text corrupt an extracted value.
- DATE FIELD — TWO-DIGIT YEAR & FUTURE-DATE GUARD: Today's date is {{CURRENT_DATE}}. A date printed with a two-digit year (e.g. "30/11/26") is frequently an OCR misread of a single digit (5↔6, 5↔8, etc.). Do NOT take it at face value if doing so would place "date" after {{CURRENT_DATE}}, or in a different year than the document's own stated period (e.g. a payslip covering "Novembre 2025" must not get a "date" in 2026). When that happens, resolve the year using the document's stated period or another fully-written four-digit-year date elsewhere in the same text instead of the ambiguous two-digit one. Never output a "date" later than {{CURRENT_DATE}} unless the document is unambiguously a future-dated instrument (e.g. an invoice due date) — those belong in "expiry_date", not "date".
- DATE vs EXPIRY_DATE — DO NOT CONFUSE THEM: "date" is always the document's OWN date — when it was issued, created, signed, or purchased/paid (e.g. an ID card's issuance date, a tax stamp's purchase date, a payslip's pay date). It is a different field from "expiry_date", which is a validity deadline, expiration date, or "valid until" date printed elsewhere in the document (e.g. an ID card's expiry, a "timbre électronique"'s validity window, an invoice's due date). If the document text states BOTH, put each in its own field — never let the expiry/validity date silently overwrite "date", and never leave "expiry_date" empty just because a validity/expiration date is clearly present in the text.
